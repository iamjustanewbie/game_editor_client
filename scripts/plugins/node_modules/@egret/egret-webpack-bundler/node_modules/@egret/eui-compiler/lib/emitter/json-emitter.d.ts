import { BaseEmitter } from ".";
import { AST_Skin, AST_NodeBase, AST_Node } from "../exml-ast";
export declare class JSONEmitter extends BaseEmitter {
    private json;
    private euiNormalizeNames;
    private elementContents;
    private elementIds;
    private skinParts;
    private states;
    private nodeMap;
    getResult(): string;
    emitHeader(themeData: any): void;
    emitSkinNode(filename: string, skinNode: AST_Skin): void;
    setPath(filename: string, json: any): void;
    setBaseState(node: AST_NodeBase, json: any, key?: string): void;
    setStates(skinNode: AST_Skin, json: any): void;
    getStatesAttribute(node: AST_NodeBase, json: any): void;
    hasAddType(node: AST_NodeBase): boolean;
    getNodeId(node: AST_NodeBase): string | null;
    parseNode(node: AST_Node): string;
    parseValue(value: string | number | boolean | AST_Node | AST_Skin): string | number | boolean | AST_Node | AST_Skin;
    convertType(type: string): string;
}
